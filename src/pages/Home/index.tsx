import DefaultPageTemplate from "templates/DefaultPageTemplate/DefaultPageTemplate";

const HomeView = () => {
  return (
    <DefaultPageTemplate crtOverlay={true} glitchNav={false}>
      <h1 className="title">Sy_nc</h1>
    </DefaultPageTemplate>
  );
};

export default HomeView;
